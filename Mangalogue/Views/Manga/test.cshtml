@model Chapter
<div class="container justify-content-center">
    @foreach (var image in Model.Pages)
    {
        <div class="d-flex flex-column w-50">
            <img src="data:image/gif;base64,@Convert.ToBase64String(image.Image)">
        </div>
    }
</div>